#just a quick functional explorerhat keyboard controlled set of motors, will build in extra functionality as and when
#01.09.19
import explorerhat
from time import sleep
import sys, tty,termios, time
explorerhat.motor.one.stop()
explorerhat.motor.two.stop()

#variables
mot1_spd = 50
mot2_spd = 50


while True:
    direction = input("f or b")
    if direction == "f":
        explorerhat.motor.one.forwards(mot1_spd)
        explorerhat.motor.two.forward(mot2_spd)
    elif direction == "b":
        explorerhat.motor.one.backward(mot1_spd)
        explorerhat.motor.two.backward(mot2_spd)
    elif direction == "s":
        explorerhat.motor.one.stop()
        explorerhat.motor.two.stop() 
    
#getch method for key pressed, turns key pressed into var
def getch():
    fd = sys.stdin.fileno()
    old_settings = termios.tcgetattr(fd)
